import { Debug } from "../../run.ts"

export const regIf = /if (.*?) {/
export const regElseIf = /else if (.*?) {/
export const regElse = /else {/


export function ExecIf(codeLine: string, code_s: string[], line: number){
    return ""
}